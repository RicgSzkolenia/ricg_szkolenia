'use strict';

/**
 * course-percs-carousel-tile router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::course-percs-carousel-tile.course-percs-carousel-tile');
