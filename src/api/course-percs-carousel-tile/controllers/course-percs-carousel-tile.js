'use strict';

/**
 * course-percs-carousel-tile controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::course-percs-carousel-tile.course-percs-carousel-tile');
